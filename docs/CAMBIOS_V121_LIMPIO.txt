CAMBIOS V121 LIMPIO
===================

1. Se reconstruye una copia limpia desde V120.

2. Se recupera el calendario en el formulario de programacion:
   - Se mantiene el campo visible con el formato elegido por el usuario.
   - Se anade un selector nativo de calendario junto al campo de fecha.
   - Al elegir fecha en el calendario, el campo visible se rellena automaticamente.
   - Al cambiar el formato de fecha, la fecha ya seleccionada se reformatea automaticamente.

3. Se conserva el envio interno al Worker en formato ISO YYYY-MM-DD.

4. Se mantienen las mejoras de V120 para vincular mantenimiento con la cuenta Cloudflare correcta.
